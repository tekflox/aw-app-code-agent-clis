---
repo: architecture
path: docs/architecture/aw-app-code-agent-clis.md
source: generated
edited: false
checksum: sha256:2bca2c3112b6487dcd911065b118abb38df4e26413d72978816c6273d6d164cf
---
# Code Agent CLIs

- **repo**: aw-app-code-agent-clis
- **layer**: app
- **technologies**: python, react
- **health** (derived): planned

Installs AI coding-agent CLIs into the workspace: Claude Code (claude), OpenAI Codex (codex), GitHub Copilot CLI (copilot), and Cursor's cursor-agent — each CLI still needs its own auth (e.g. `claude login`, `codex login`, `copilot`, `cursor-agent login`) run from a workspace terminal before use. Also owns the Agents nav menu (launch/resume sessions per CLI type) and the agent-session-history API/DB backing it — decoupled from aw-workspace core per the 2026-08-03 architecture decision.

## Connections
- `db` → **postgres** — app-owned tables in the workspace schema
- `http` → **aw-workspace** — routes mounted at /api/apps/code-agent-clis
- `other` → **aw-app-essentials** — claude/codex/copilot are npm-published packages, installed through the Node

## MCP tools
_none exposed_

## Requirements
### O nome da sessão é a primeira fala real, não o texto que a CLI injetou
- Given o Claude injeta um bloco automático no começo da sessão ("Caveat: The messages below were generated by the automated tool"), que fica gravado como se fosse mensagem do usuário
- When o nome é derivado percorrendo os registros e limpando as tags injetadas (repos/aw-app-code-agent-clis/code_agent_clis_app/sessions.py::_first_user_message e _strip_injected_tags)
- Then o bloco injetado é reduzido a vazio, a varredura continua até achar texto de verdade, e o seletor mostra o que a pessoa realmente pediu — sem isso toda sessão aparece com o mesmo nome genérico do aviso, e uma lista onde todos os itens têm o mesmo nome é funcionalmente uma lista sem nomes
- intended_status: `not_implemented` · derived health: `not_implemented`
- tests: `repos/aw-app-code-agent-clis/tests/test_first_user_message.py` (passing)

### Tag truncada é tratada como ruído, não como texto
- Given uma mensagem cortada em algum ponto acima, deixando a tag de abertura sem a de fechamento
- When a limpeza roda sobre esse conteúdo (repos/aw-app-code-agent-clis/code_agent_clis_app/sessions.py::_strip_injected_tags, coberto por tests/test_first_user_message.py::test_strip_injected_tags_handles_truncated_tag:42)
- Then o resultado é vazio, do mesmo jeito que na tag completa — casar só o par fechado deixaria o fragmento passar como texto legítimo, e o nome da sessão viraria meia frase do aviso automático, que é ainda mais confuso que o aviso inteiro porque parece conteúdo. Quando não há mensagem real nenhuma, o retorno é None e não string vazia
- intended_status: `not_implemented` · derived health: `not_implemented`
- tests: `repos/aw-app-code-agent-clis/tests/test_first_user_message.py` (passing)

### Esconder uma sessão vale por tipo de CLI e é reversível
- Given várias CLIs coexistem no app e a mesma sessão pode ter identificador colidente entre elas
- When uma sessão é escondida e depois restaurada (repos/aw-app-code-agent-clis/code_agent_clis_app/sessions.py, via tests/test_sessions_and_routes.py::test_hide_session_removes_it_from_listing:98, test_restore_hidden_session_brings_it_back:107 e test_hidden_session_scoped_per_type:116)
- Then ela some da listagem, volta ao ser restaurada, e o esconderijo é escopado por tipo de CLI — sem o escopo, esconder uma sessão do Claude sumiria com a sessão de mesmo id do Codex, que é uma perda silenciosa de algo que ninguém pediu para esconder. Esconder nunca apaga: é filtro de listagem, e por isso a restauração pode existir
- intended_status: `not_implemented` · derived health: `not_implemented`
- tests: `repos/aw-app-code-agent-clis/tests/test_sessions_and_routes.py` (passing)

### Sessões vêm da mais recente para a mais antiga, e tipo desconhecido devolve lista vazia
- Given um pedido de listagem que pode nomear uma CLI que este app não conhece
- When a listagem é montada e ordenada (repos/aw-app-code-agent-clis/code_agent_clis_app/sessions.py, via tests/test_sessions_and_routes.py::test_list_sessions_sorted_by_updated_at_desc:85 e test_list_sessions_unknown_type_returns_empty:92)
- Then a ordem é por updated_at decrescente e um tipo desconhecido devolve lista vazia em vez de erro — lista vazia é a resposta certa porque a pergunta ("quais sessões desta CLI existem?") tem resposta, e ela é nenhuma; um erro obrigaria todo chamador a distinguir dois casos que levam à mesma tela. A ordem decrescente é o que faz a sessão em que se estava trabalhando aparecer primeiro
- intended_status: `not_implemented` · derived health: `not_implemented`
- tests: `repos/aw-app-code-agent-clis/tests/test_sessions_and_routes.py` (passing)
