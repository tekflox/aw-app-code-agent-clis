{
  "manifest_version": 1,
  "id": "code-agent-clis",
  "name": "Code Agent CLIs",
  "version": "0.7.0",
  "description": "Installs AI coding-agent CLIs into the workspace: Claude Code (claude), OpenAI Codex (codex), GitHub Copilot CLI (copilot), and Cursor's cursor-agent \u2014 each CLI still needs its own auth (e.g. `claude login`, `codex login`, `copilot`, `cursor-agent login`) run from a workspace terminal before use. Also owns the Agents nav menu (launch/resume sessions per CLI type) and the agent-session-history API/DB backing it \u2014 decoupled from aw-workspace core per the 2026-08-03 architecture decision.",
  "category": "Runnables",
  "tier": "inprocess",
  "publisher": "TekFlox",
  "resource_estimate": {
    "cpu": "low",
    "memory": "low",
    "disk": "medium"
  },
  "runtime": {
    "python": ">=3.11",
    "entrypoint": "code_agent_clis_app.plugin:CodeAgentClisAppPlugin",
    "pip_requires": []
  },
  "permissions": [
    "commands:install",
    "fs:workspace-data",
    "net:outbound",
    "routes:register",
    "db:own-tables",
    "ui:code",
    "ui:slots:core.nav"
  ],
  "contributes": {
    "system_clis": [
      {
        "name": "claude",
        "installer": "scripts/install_claude.sh"
      },
      {
        "name": "codex",
        "installer": "scripts/install_codex.sh"
      },
      {
        "name": "copilot",
        "installer": "scripts/install_copilot.sh"
      },
      {
        "name": "cursor-agent",
        "installer": "scripts/install_cursor.sh"
      }
    ],
    "routes": [
      {
        "prefix": "/api/apps/code-agent-clis"
      }
    ],
    "frontend": {
      "mode": "component",
      "bundle": "ui/dist/code-agent-clis.js",
      "slots": [
        "core.nav"
      ]
    }
  },
  "dependencies": {
    "apps": [
      {
        "id": "essentials",
        "version": ">=0.8.0",
        "required": true,
        "reason": "claude/codex/copilot are npm-published packages, installed through the Node.js dev toolkit (nvm/node/npm) that the essentials app owns \u2014 it must be installed/loaded first."
      }
    ],
    "note": "Runtime-enforced app dependency: aw-workspace installs/loads essentials before code-agent-clis and keeps essentials loaded while code-agent-clis is desired."
  },
  "migrations": {}
}
